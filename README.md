# Sparta-C-Sharp-Course
C#

# **Arithmetic Operators**

So what can we do with numerical data? A first step is to write expressions using *arithmetic operators*.

Arithmetic operators include:

- addition `+`
- subtraction `-`
- multiplication `*`
- division `/`

    Console.WriteLine(5 / 3);
    Console.WriteLine(5 / 3.0);
    // prints 1
    // prints 1.66667

    int apple = 0;
    apple++;
    Console.Write(apple); // prints 1
    
    //++ means increment by 1
    // -- means decrement by 1 

    int apple = 0;
    apple += 3; // is the same as apple = apple + 3
    Console.Write(apple); // prints 3
    
    //above is an example of increment by 3 "+=" swap the signs and it will be = apple +3